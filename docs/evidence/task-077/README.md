# TASK-077 gate-v2 evidence

This directory stores local release gate v2 evidence artifacts produced by:

- `./scripts/multyagents gate-v2`

Generated files (JSON/MD/log) are intentionally ignored in git to avoid noisy diffs.
Use `latest.json` and `latest.md` for current local state.
