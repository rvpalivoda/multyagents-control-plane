# TASK-073 restart persistence evidence

This directory stores local restart-persistence evidence artifacts produced by:

- `./scripts/multyagents restart-persistence`

Generated files (JSON/MD/log) are intentionally ignored in git to avoid noisy diffs.
Use `latest.json`, `latest.md`, and `latest-pytest.log` for current local state.
