# TASK-076 SLO evidence

This directory stores local SLO load/soak evidence artifacts produced by:

- `./scripts/multyagents slo-smoke`

Generated JSON/Markdown/pytest logs are ignored in git to keep diffs clean.
Use `latest.json`, `latest.md`, and `latest-pytest.log` for current local state.
